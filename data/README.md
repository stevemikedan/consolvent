# Data Outputs

Generated experiment logs and constraint snapshots live here during local runs.

The public repo should not treat bulk validation outputs as source code. Regenerate them with:

```bash
make reify
```

If a small fixture dataset is needed for tests or examples, add it deliberately under a non-ignored fixture path.

